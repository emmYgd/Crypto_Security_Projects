import javax.crypto.*
import java.security.*
/* Mac myMac = Mac.getInstance("HmacSHA512")
 assert "Ade" instanceof String == true 
 assert myMac instanceof Mac == true */
 
// Signature PrivateKeySignsMAC = Signature.getInstance("RSASHA512")
 
 Cipher PrivKeyEncSecKey = Cipher.getInstance("EC");